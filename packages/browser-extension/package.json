{
  "name": "@think-prompt/browser-extension",
  "version": "0.3.1",
  "description": "Think-Prompt browser extension — local-first prompt coach for ChatGPT, Claude, Gemini, and more.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "build:prod": "BUILD_MODE=production node build.mjs",
    "package": "BUILD_MODE=production node build.mjs && node scripts/package.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@think-prompt/rules": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/chrome": "^0.0.290",
    "esbuild": "^0.28.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
